@page "/webapp/code"
@inject IAppDataSingleton AppData

<PageHeader Page="@Page" />

<Paragraph Margin="Margin.Is3.FromTop" TextAlignment="TextAlignment.Center">
    The 'WebApp' is written in c# with blazor server and can be found on <Link To="https://github.com/EifelMono/TausendblattGuest">github</Link>.
</Paragraph>

<UnderConstruction></UnderConstruction>

@code {
    [Parameter]
    public string? Route { get; set; }

    [Parameter]
    public Page? Page { get; set; }

    protected override void OnInitialized()
    {
        base.OnInitialized();
        Page = AppData.Pages.WebAppCode;
        Route = Page.Route;
    }
}
