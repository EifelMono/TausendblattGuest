@page "/webapp/code"
@inject IAppDataSingleton AppData

<PageHeader Page="@Page" />


<Paragraph TextAlignment="TextAlignment.Center">
    The code is written in c# with blazor server and can be found on <Link To="https://github.com/EifelMono/TausendblattGuest">github</Link>.
</Paragraph>

<UnderConstruction></UnderConstruction>

@code {
    [Parameter]
    public string? Route { get; set; }

    [Parameter]
    public Page? Page { get; set; }

    protected override void OnInitialized()
    {
        base.OnInitialized();
        Page = AppData.Pages.WebAppCode;
        Route = Page.Route;
    }
}
